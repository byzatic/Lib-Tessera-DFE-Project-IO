package io.github.byzatic.tessera.lib.configio.service_spi;

/** Value type of a service configuration parameter. */
public enum ServiceParameterType {
    STRING,
    INTEGER,
    LONG,
    DECIMAL,
    BOOLEAN,
    SELECT
}
